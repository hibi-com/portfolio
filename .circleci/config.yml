version: 2.1
orbs:
  node: circleci/node@7.1.0
  browser-tools: circleci/browser-tools@1.5.0
executors:
  default:
    docker:
      - image: cimg/node:22.12.0@sha256:0dae71c56fb15a93a8223b5bfe2e93395b97efbf5509a48fe75b890bf1ad0b4e
    resource_class: large
    working_directory: ~/project
  large:
    docker:
      - image: cimg/node:22.12.0-browsers@sha256:6efb5d0b817685f4c28c400a780cec9fccd8059907375305a31725b4b530cdb3
    resource_class: large
    working_directory: ~/project
  machine-large:
    machine:
      image: ubuntu-2204:2024.11.1
    resource_class: large
    working_directory: ~/project
commands:
  install-bun:
    description: Install Bun runtime (version from package.json's packageManager)
    steps:
      - run:
          name: Install Bun
          command: |
            BUN_VERSION="$(python3 -c "import json; print(json.load(open('package.json'))['packageManager'].removeprefix('bun@'))")"
            curl -fsSL https://bun.sh/install | bash -s "bun-v${BUN_VERSION}"
            echo 'export BUN_INSTALL="$HOME/.bun"' >> $BASH_ENV
            echo 'export PATH="$BUN_INSTALL/bin:$PATH"' >> $BASH_ENV
            source $BASH_ENV
            bun --version
  load-env-config:
    description: Load and decode environment configuration from CircleCI variables
    parameters:
      environment:
        type: enum
        enum: [rc, stg, prd]
    steps:
      - run:
          name: Decode environment configuration for << parameters.environment >>
          command: |
            ENV_VAR_NAME="ENV_CONFIG_<< parameters.environment >>"
            ENV_VAR_NAME="${ENV_VAR_NAME^^}"
            case "<< parameters.environment >>" in
              rc)
                ENV_CONFIG="${ENV_CONFIG_RC}"
                ;;
              stg)
                ENV_CONFIG="${ENV_CONFIG_STG}"
                ;;
              prd)
                ENV_CONFIG="${ENV_CONFIG_PRD}"
                ;;
            esac
            if [ -z "${ENV_CONFIG}" ]; then
              exit 1
            fi
            mkdir -p infra
            echo "${ENV_CONFIG}" | base64 -d > infra/env.yaml
  restore-deps:
    description: Restore cached dependencies
    steps:
      - restore_cache:
          keys:
            - bun-deps-v1-{{ checksum "bun.lock" }}
            - bun-deps-v1-
  save-deps:
    description: Save dependencies to cache
    steps:
      - save_cache:
          key: bun-deps-v1-{{ checksum "bun.lock" }}
          paths:
            - node_modules
            - ~/.bun
  restore-turbo:
    description: Restore Turborepo cache
    steps:
      - restore_cache:
          keys:
            - turbo-cache-v1-{{ .Branch }}-{{ .Revision }}
            - turbo-cache-v1-{{ .Branch }}-
            - turbo-cache-v1-
  save-turbo:
    description: Save Turborepo cache
    steps:
      - save_cache:
          key: turbo-cache-v1-{{ .Branch }}-{{ .Revision }}
          paths:
            - .turbo
  login-pulumi-r2:
    description: Login to Pulumi DIY backend on Cloudflare R2
    steps:
      - run:
          name: Login Pulumi (R2 backend)
          command: |
            for v in AWS_ACCESS_KEY_ID AWS_SECRET_ACCESS_KEY PULUMI_BACKEND_URL PULUMI_CONFIG_PASSPHRASE; do
              if [ -z "${!v:-}" ]; then
                exit 1
              fi
            done
            pulumi login "${PULUMI_BACKEND_URL}"
            pulumi whoami -v || true
  prepare-infra-env:
    description: Decode ENV_CONFIG and materialize infra/.env for Pulumi
    parameters:
      environment:
        type: enum
        enum: [rc, stg, prd]
    steps:
      - load-env-config:
          environment: << parameters.environment >>
      - run:
          name: Materialize infra/.env from env.yaml
          working_directory: infra
          command: |
            bun run scripts/env-yaml-to-dotenv.mjs
            for v in CLOUDFLARE_API_TOKEN CLOUDFLARE_ACCOUNT_ID CLOUDFLARE_ZONE_ID SENTRY_AUTH_TOKEN SENTRY_ORG; do
              if ! grep -q "^${v}=.\+" .env; then
                exit 1
              fi
            done
jobs:
  setup:
    executor: default
    steps:
      - checkout
      - install-bun
      - restore-deps
      - run:
          name: Install dependencies
          command: |
            bun install --frozen-lockfile --ignore-scripts
            bun run generate --filter='!@portfolio/infra'
            bun run build
          no_output_timeout: 20m
      - save-deps
      - restore-turbo
      - save-turbo
      - persist_to_workspace:
          root: .
          paths:
            - .
            - node_modules
            - packages/*/node_modules
            - apps/*/node_modules
  format:
    executor: machine-large
    steps:
      - attach_workspace:
          at: .
      - install-bun
      - run:
          name: Format check
          command: bun run fmt:check
  lint:
    executor: machine-large
    steps:
      - attach_workspace:
          at: .
      - install-bun
      - run:
          name: Lint
          command: bun run lint
  typecheck:
    executor: default
    steps:
      - attach_workspace:
          at: .
      - install-bun
      - run:
          name: Typecheck
          command: bun run typecheck
  test:
    executor: default
    steps:
      - attach_workspace:
          at: .
      - install-bun
      - run:
          name: Run unit tests
          command: bun run test
      - store_test_results:
          path: .results
      - run:
          name: Create GitHub Issue on failure
          when: on_fail
          command: |
            if [ -n "${GITHUB_TOKEN}" ]; then
              curl -X POST \
                -H "Authorization: token ${GITHUB_TOKEN}" \
                -H "Accept: application/vnd.github.v3+json" \
                https://api.github.com/repos/${CIRCLE_PROJECT_USERNAME}/${CIRCLE_PROJECT_REPONAME}/issues \
                -d "{
                  \"title\": \"[CI Failure] Unit tests failed on ${CIRCLE_BRANCH}\",
                  \"body\": \"## CI Failure Report\n\n**Job:** test\n**Branch:** ${CIRCLE_BRANCH}\n**Commit:** ${CIRCLE_SHA1}\n**Build:** [#${CIRCLE_BUILD_NUM}](${CIRCLE_BUILD_URL})\n\n### Actions Required\n\n- [ ] Review the build logs\n- [ ] Fix the failing tests\n- [ ] Re-run the CI pipeline\",
                  \"labels\": [\"ci-failure\", \"automated\"]
                }"
            fi
  integration:
    executor: machine-large
    steps:
      - attach_workspace:
          at: .
      - run:
          name: Setup libSQL
          command: |
            docker compose -f compose.yaml up -d --wait sqlite
            docker compose -f compose.yaml run --rm setup-db
      - run:
          name: Run integration tests
          command: docker compose -f compose.ci.yaml run --rm --no-deps integration
      - run:
          name: Tear down libSQL
          when: always
          command: docker compose -f compose.yaml stop sqlite || true
      - store_test_results:
          path: .results
      - run:
          name: Create GitHub Issue on failure
          when: on_fail
          command: |
            if [ -n "${GITHUB_TOKEN}" ]; then
              curl -X POST \
                -H "Authorization: token ${GITHUB_TOKEN}" \
                -H "Accept: application/vnd.github.v3+json" \
                https://api.github.com/repos/${CIRCLE_PROJECT_USERNAME}/${CIRCLE_PROJECT_REPONAME}/issues \
                -d "{
                  \"title\": \"[CI Failure] Integration tests failed on ${CIRCLE_BRANCH}\",
                  \"body\": \"## CI Failure Report\n\n**Job:** integration\n**Branch:** ${CIRCLE_BRANCH}\n**Commit:** ${CIRCLE_SHA1}\n**Build:** [#${CIRCLE_BUILD_NUM}](${CIRCLE_BUILD_URL})\n\n### Actions Required\n\n- [ ] Review the build logs\n- [ ] Fix the failing tests\n- [ ] Re-run the CI pipeline\",
                  \"labels\": [\"ci-failure\", \"automated\"]
                }"
            fi
  security-scan:
    executor: machine-large
    steps:
      - attach_workspace:
          at: .
      - run:
          name: Secret detection (gitleaks via compose.ci.yaml)
          command: |
            set +e
            docker compose -f compose.ci.yaml run --rm gitleaks
            GITLEAKS_EXIT=$?
            set -e
            if [ -f "gitleaks-report.json" ]; then
              SECRETS_FOUND=$(python3 -c "import json;from pathlib import Path;d=json.loads(Path('gitleaks-report.json').read_text() or '[]');print(len(d) if isinstance(d,list) else (len(d.get('Findings') or d.get('findings') or []) if isinstance(d,dict) else 0))")
              if [ "${SECRETS_FOUND}" -gt "0" ]; then
                exit 1
              fi
            elif [ "${GITLEAKS_EXIT}" -ne 0 ]; then
              exit "${GITLEAKS_EXIT}"
            fi
      - run:
          name: Filesystem vulnerability scan (trivy via compose.ci.yaml)
          command: |
            docker compose -f compose.ci.yaml run --rm trivy
            HIGH_VULNS=$(python3 -c "import json;from pathlib import Path;p=Path('trivy-report.json');d=json.loads(p.read_text() or '{}') if p.exists() else {};print(sum(1 for r in d.get('Results') or [] for v in r.get('Vulnerabilities') or [] if v.get('Severity') in ('HIGH','CRITICAL')))")
            if [ "${HIGH_VULNS}" -gt "0" ]; then
              exit 1
            fi
      - run:
          name: Dependency vulnerability scan (snyk via compose.ci.yaml)
          command: |
            if [ -z "${SNYK_TOKEN}" ]; then
              exit 0
            fi
            docker compose -f compose.ci.yaml run --rm -e SNYK_TOKEN snyk || true
            if [ ! -f "snyk-report.json" ]; then
              exit 0
            fi
            HIGH_VULNS=$(python3 -c "import json;from pathlib import Path;d=json.loads(Path('snyk-report.json').read_text() or '{}');print(sum(1 for v in d.get('vulnerabilities') or [] if v.get('severity') in ('high','critical')))")
            if [ "${HIGH_VULNS}" -gt "0" ]; then
              exit 1
            fi
      - store_artifacts:
          path: snyk-report.json
          destination: security/snyk-report.json
      - store_artifacts:
          path: trivy-report.json
          destination: security/trivy-report.json
      - store_artifacts:
          path: gitleaks-report.json
          destination: security/gitleaks-report.json
      - run:
          name: Create GitHub Issue on security failure
          when: on_fail
          command: |
            if [ -n "${GITHUB_TOKEN}" ]; then
              curl -X POST \
                -H "Authorization: token ${GITHUB_TOKEN}" \
                -H "Accept: application/vnd.github.v3+json" \
                https://api.github.com/repos/${CIRCLE_PROJECT_USERNAME}/${CIRCLE_PROJECT_REPONAME}/issues \
                -d "{
                  \"title\": \"[Security Alert] Vulnerabilities detected on ${CIRCLE_BRANCH}\",
                  \"body\": \"## Security Scan Failure\n\n**Branch:** ${CIRCLE_BRANCH}\n**Commit:** ${CIRCLE_SHA1}\n**Build:** [#${CIRCLE_BUILD_NUM}](${CIRCLE_BUILD_URL})\n\n### Actions Required\n\n- [ ] Review security scan reports in artifacts\n- [ ] Update vulnerable dependencies\n- [ ] Remove detected secrets\n- [ ] Re-run security scan\n\n### Reports\n\n- Snyk: Check artifacts/security/snyk-report.json\n- Trivy: Check artifacts/security/trivy-report.json\n- Gitleaks: Check artifacts/security/gitleaks-report.json\",
                  \"labels\": [\"security\", \"vulnerability\", \"automated\"]
                }"
            fi
  e2e:
    executor: large
    steps:
      - attach_workspace:
          at: .
      - install-bun
      - browser-tools/install-chrome
      - browser-tools/install-chromedriver
      - run:
          name: Install Playwright browsers
          command: |
            bun add -g @playwright/test@1.57.0
            bunx playwright install --with-deps chromium
      - run:
          name: Run E2E tests
          command: bun run e2e
          no_output_timeout: 20m
      - store_artifacts:
          path: docs/playwright/report
          destination: playwright-report
      - store_test_results:
          path: .results
      - run:
          name: Create GitHub Issue on failure
          when: on_fail
          command: |
            if [ -n "${GITHUB_TOKEN}" ]; then
              curl -X POST \
                -H "Authorization: token ${GITHUB_TOKEN}" \
                -H "Accept: application/vnd.github.v3+json" \
                https://api.github.com/repos/${CIRCLE_PROJECT_USERNAME}/${CIRCLE_PROJECT_REPONAME}/issues \
                -d "{
                  \"title\": \"[CI Failure] E2E tests failed on ${CIRCLE_BRANCH}\",
                  \"body\": \"## CI Failure Report\n\n**Job:** e2e\n**Branch:** ${CIRCLE_BRANCH}\n**Commit:** ${CIRCLE_SHA1}\n**Build:** [#${CIRCLE_BUILD_NUM}](${CIRCLE_BUILD_URL})\n\n### Actions Required\n\n- [ ] Review the Playwright report\n- [ ] Fix the failing E2E tests\n- [ ] Re-run the CI pipeline\",
                  \"labels\": [\"ci-failure\", \"e2e\", \"automated\"]
                }"
            fi
  build:
    executor: default
    steps:
      - attach_workspace:
          at: .
      - install-bun
      - restore-turbo
      - run:
          name: Build application
          command: bun run build
      - save-turbo
      - persist_to_workspace:
          root: .
          paths:
            - apps/*/dist
            - apps/*/build
            - packages/*/dist
      - run:
          name: Create GitHub Issue on failure
          when: on_fail
          command: |
            if [ -n "${GITHUB_TOKEN}" ]; then
              curl -X POST \
                -H "Authorization: token ${GITHUB_TOKEN}" \
                -H "Accept: application/vnd.github.v3+json" \
                https://api.github.com/repos/${CIRCLE_PROJECT_USERNAME}/${CIRCLE_PROJECT_REPONAME}/issues \
                -d "{
                  \"title\": \"[CI Failure] Build failed on ${CIRCLE_BRANCH}\",
                  \"body\": \"## CI Failure Report\n\n**Job:** build\n**Branch:** ${CIRCLE_BRANCH}\n**Commit:** ${CIRCLE_SHA1}\n**Build:** [#${CIRCLE_BUILD_NUM}](${CIRCLE_BUILD_URL})\n\n### Actions Required\n\n- [ ] Review the build logs\n- [ ] Fix the build errors\n- [ ] Re-run the CI pipeline\",
                  \"labels\": [\"ci-failure\", \"build\", \"automated\"]
                }"
            fi
  upload-artifacts:
    executor: default
    steps:
      - attach_workspace:
          at: .
      - run:
          name: Package build artifacts for CircleCI
          command: |
            VERSION="${CIRCLE_SHA1:0:7}-$(date +%s)"
            echo "export ARTIFACT_VERSION=${VERSION}" >> $BASH_ENV
            source $BASH_ENV

            echo "${VERSION}" > .artifact-version
            echo "${CIRCLE_SHA1}" > .artifact-sha

            mkdir -p ci-artifacts/versions/${ARTIFACT_VERSION} ci-artifacts/${CIRCLE_SHA1}

            for app in web api admin wiki; do
              for kind in dist build; do
                SRC="apps/${app}/${kind}"
                if [ -d "${SRC}" ]; then
                  mkdir -p "ci-artifacts/versions/${ARTIFACT_VERSION}/${app}" "ci-artifacts/${CIRCLE_SHA1}/${app}"
                  cp -R "${SRC}" "ci-artifacts/versions/${ARTIFACT_VERSION}/${app}/${kind}"
                  cp -R "${SRC}" "ci-artifacts/${CIRCLE_SHA1}/${app}/${kind}"
                fi
              done
            done

            cat > manifest.json \<<EOF
            {
              "version": "${ARTIFACT_VERSION}",
              "sha": "${CIRCLE_SHA1}",
              "branch": "${CIRCLE_BRANCH}",
              "build_num": ${CIRCLE_BUILD_NUM},
              "timestamp": "$(date -u +%Y-%m-%dT%H:%M:%SZ)",
              "environment": "rc",
              "git_author": "${CIRCLE_USERNAME}",
              "build_url": "${CIRCLE_BUILD_URL}"
            }
            EOF

            cp manifest.json "ci-artifacts/versions/${ARTIFACT_VERSION}/manifest.json"
            cp manifest.json "ci-artifacts/${CIRCLE_SHA1}/manifest.json"
            mkdir -p ci-artifacts/latest-rc
            cp manifest.json ci-artifacts/latest-rc/manifest.json
      - persist_to_workspace:
          root: .
          paths:
            - .artifact-version
            - .artifact-sha
            - manifest.json
            - ci-artifacts
            - apps/web/dist
            - apps/web/build
            - apps/api/dist
            - apps/admin/dist
            - apps/admin/build
            - apps/wiki/dist
            - apps/wiki/build
      - store_artifacts:
          path: ci-artifacts
          destination: build-artifacts
  download-artifacts:
    executor: default
    parameters:
      commit_sha:
        type: string
        default: ""
      artifact_version:
        type: string
        default: ""
      environment:
        type: enum
        enum: [rc, stg, prd]
        default: rc
    steps:
      - checkout
      - install-bun
      - run:
          name: Prepare artifacts (rebuild from source)
          command: |
            bun install --frozen-lockfile --ignore-scripts
            bun run build

            mkdir -p artifacts
            for app in web api admin wiki; do
              for kind in dist build; do
                SRC="apps/${app}/${kind}"
                if [ -d "${SRC}" ]; then
                  mkdir -p "artifacts/${app}"
                  cp -R "${SRC}" "artifacts/${app}/${kind}"
                fi
              done
            done

            cat > manifest.json \<<EOF
            {
              "version": "${CIRCLE_SHA1:0:7}-rebuild",
              "sha": "${CIRCLE_SHA1}",
              "branch": "${CIRCLE_BRANCH}",
              "build_num": ${CIRCLE_BUILD_NUM},
              "timestamp": "$(date -u +%Y-%m-%dT%H:%M:%SZ)",
              "environment": "<< parameters.environment >>",
              "source": "rebuild"
            }
            EOF

            cp manifest.json artifacts/manifest.json
      - persist_to_workspace:
          root: .
          paths:
            - artifacts
            - manifest.json
            - apps/web/dist
            - apps/web/build
            - apps/api/dist
            - apps/admin/dist
            - apps/admin/build
            - apps/wiki/dist
            - apps/wiki/build
      - store_artifacts:
          path: artifacts
          destination: deploy-artifacts-<< parameters.environment >>
  artifact-promotion:
    executor: default
    parameters:
      from_environment:
        type: enum
        enum: [rc, stg]
      to_environment:
        type: enum
        enum: [stg, prd]
    steps:
      - attach_workspace:
          at: .
      - run:
          name: Record promotion << parameters.from_environment >> to << parameters.to_environment >>
          command: |
            if [ -f manifest.json ]; then
              FROM_MANIFEST=manifest.json
            elif [ -f artifacts/manifest.json ]; then
              FROM_MANIFEST=artifacts/manifest.json
            else
              echo '{"version":"unknown","sha":"'"${CIRCLE_SHA1}"'"}' > from-manifest.json
              FROM_MANIFEST=from-manifest.json
            fi

            jq --arg env "<< parameters.to_environment >>" \
               --arg from "<< parameters.from_environment >>" \
               --arg at "$(date -u +%Y-%m-%dT%H:%M:%SZ)" \
               '.environment = $env | .promoted_at = $at | .promoted_from = $from' \
               "${FROM_MANIFEST}" > to-manifest.json

            mkdir -p "ci-artifacts/latest-<< parameters.to_environment >>"
            cp to-manifest.json "ci-artifacts/latest-<< parameters.to_environment >>/manifest.json"
            cat to-manifest.json | jq '.'
      - persist_to_workspace:
          root: .
          paths:
            - to-manifest.json
            - ci-artifacts
      - store_artifacts:
          path: to-manifest.json
          destination: promotion-<< parameters.to_environment >>
  db-migrate:
    executor: default
    parameters:
      environment:
        type: enum
        enum: [rc, stg, prd]
    steps:
      - attach_workspace:
          at: .
      - install-bun
      - load-env-config:
          environment: << parameters.environment >>
      - run:
          name: Apply D1 migrations for << parameters.environment >>
          working_directory: packages/db
          command: |
            bun add -g wrangler@4.54.0
            DB_NAME="${D1_DATABASE_NAME:-portfolio-<< parameters.environment >>-db}"
            bunx wrangler d1 migrations apply "${DB_NAME}" --remote
      - run:
          name: Create GitHub Issue on failure
          when: on_fail
          command: |
            if [ -n "${GITHUB_TOKEN}" ]; then
              curl -X POST \
                -H "Authorization: token ${GITHUB_TOKEN}" \
                -H "Accept: application/vnd.github.v3+json" \
                https://api.github.com/repos/${CIRCLE_PROJECT_USERNAME}/${CIRCLE_PROJECT_REPONAME}/issues \
                -d "{
                  \"title\": \"[Migration Failure] D1 migration failed on << parameters.environment >>\",
                  \"body\": \"## D1 Migration Failure\n\n**Environment:** << parameters.environment >>\n**Branch:** ${CIRCLE_BRANCH}\n**Commit:** ${CIRCLE_SHA1}\n**Build:** [#${CIRCLE_BUILD_NUM}](${CIRCLE_BUILD_URL})\",
                  \"labels\": [\"migration-failure\", \"<< parameters.environment >>\", \"automated\"]
                }"
            fi
  pre-deploy-check:
    executor: default
    parameters:
      environment:
        type: enum
        enum: [rc, stg, prd]
    steps:
      - attach_workspace:
          at: .
      - install-bun
      - load-env-config:
          environment: << parameters.environment >>
      - run:
          name: Pre-deployment validation
          command: |
            if [ ! -d "artifacts/${CIRCLE_SHA1}" ] && [ ! -f "manifest.json" ]; then
              exit 1
            fi
            if [ ! -f "infra/env.yaml" ]; then
              exit 1
            fi
  preview-deploy:
    executor: default
    parameters:
      environment:
        type: enum
        enum: [rc, stg, prd]
    steps:
      - attach_workspace:
          at: .
      - install-bun
      - load-env-config:
          environment: << parameters.environment >>
      - run:
          name: Install Wrangler CLI
          command: |
            bun add -g wrangler@4.54.0
            wrangler --version
      - run:
          name: Deploy preview for << parameters.environment >>
          command: |
            bun run deploy -- --env=preview
      - persist_to_workspace:
          root: .
          paths:
            - .preview-url
  smoke-test:
    executor: default
    parameters:
      environment:
        type: enum
        enum: [rc, stg, prd]
    steps:
      - attach_workspace:
          at: .
      - install-bun
      - load-env-config:
          environment: << parameters.environment >>
      - run:
          name: Run smoke tests on preview
          command: |
            if [ -f ".preview-url" ]; then
              PREVIEW_URL=$(cat .preview-url)
            else
              case "<< parameters.environment >>" in
                rc) PREVIEW_URL="https://rc.www.ageha734.jp" ;;
                stg) PREVIEW_URL="https://stg.www.ageha734.jp" ;;
                prd) PREVIEW_URL="https://www.ageha734.jp" ;;
              esac
            fi
            if ! curl -sSf "${PREVIEW_URL}/" > /dev/null 2>&1; then
              exit 1
            fi
  validate-deployment:
    executor: default
    parameters:
      environment:
        type: enum
        enum: [rc, stg, prd]
      monitoring_duration:
        type: string
        default: "15m"
    steps:
      - attach_workspace:
          at: .
      - install-bun
      - load-env-config:
          environment: << parameters.environment >>
      - run:
          name: Comprehensive deployment validation for << parameters.environment >>
          command: |
            case "<< parameters.environment >>" in
              rc)
                BASE_URL="https://rc.www.ageha734.jp"
                API_URL="https://rc.api.ageha734.jp"
                ;;
              stg)
                BASE_URL="https://stg.www.ageha734.jp"
                API_URL="https://stg.api.ageha734.jp"
                ;;
              prd)
                BASE_URL="https://www.ageha734.jp"
                API_URL="https://api.ageha734.jp"
                ;;
            esac

            if ! curl -sSf "${BASE_URL}/" > /dev/null 2>&1; then
              exit 1
            fi

            if ! curl -sSf "${API_URL}/health" | jq -e '.status == "healthy"' > /dev/null 2>&1; then
              exit 1
            fi

            if [ -d "apps/e2e" ]; then
              bunx playwright test --grep "@critical" --reporter=line || {
                exit 1
              }
            else
              exit 1
            fi

            if [ -n "${SENTRY_AUTH_TOKEN}" ]; then
              CURRENT_ERRORS=$(curl -s "https://sentry.io/api/0/organizations/${SENTRY_ORG}/stats_v2/?statsPeriod=15m&interval=15m&field=sum(quantity)&category=error" \
                -H "Authorization: Bearer ${SENTRY_AUTH_TOKEN}" | jq '.[0].series."sum(quantity)"[0] // 0')

              BASELINE_ERRORS=$(curl -s "https://sentry.io/api/0/organizations/${SENTRY_ORG}/stats_v2/?statsPeriod=1h&interval=1h&field=sum(quantity)&category=error" \
                -H "Authorization: Bearer ${SENTRY_AUTH_TOKEN}" | jq '.[0].series."sum(quantity)"[0] // 0')

              THRESHOLD=$(echo "${BASELINE_ERRORS} * 1.5" | bc)

              if (( $(echo "${CURRENT_ERRORS} > ${THRESHOLD}" | bc -l) )); then
                exit 1
              fi
            else
              exit 1
            fi

            if command -v lighthouse > /dev/null 2>&1; then
              lighthouse "${BASE_URL}" \
                --only-categories=performance \
                --preset=desktop \
                --chrome-flags="--headless" \
                --output=json \
                --output-path=./lighthouse-report.json \
                --quiet

              PERFORMANCE_SCORE=$(jq '.categories.performance.score * 100' lighthouse-report.json)

              if (( $(echo "${PERFORMANCE_SCORE} < 80" | bc -l) )); then
                exit 1
              fi
            else
              exit 1
            fi

            if [ -z "${CLOUDFLARE_API_TOKEN}" ] || ! command -v wrangler > /dev/null 2>&1; then
              exit 1
            fi

            if [ "<< parameters.environment >>" = "prd" ]; then
              if [ -n "${SENTRY_AUTH_TOKEN}" ]; then
                CURRENT_USERS=$(curl -s "https://sentry.io/api/0/organizations/${SENTRY_ORG}/stats_v2/?statsPeriod=1h&interval=1h&field=count_unique(user)&category=transaction" \
                  -H "Authorization: Bearer ${SENTRY_AUTH_TOKEN}" | jq '.[0].series."count_unique(user)"[0] // 0')

                BASELINE_USERS=$(curl -s "https://sentry.io/api/0/organizations/${SENTRY_ORG}/stats_v2/?statsPeriod=24h&interval=24h&field=count_unique(user)&category=transaction" \
                  -H "Authorization: Bearer ${SENTRY_AUTH_TOKEN}" | jq '.[0].series."count_unique(user)"[0] // 0')

                MIN_USERS=$(echo "${BASELINE_USERS} * 0.8" | bc)

                if (( $(echo "${CURRENT_USERS} < ${MIN_USERS}" | bc -l) )); then
                  exit 1
                fi
              else
                exit 1
              fi
            fi
      - store_artifacts:
          path: lighthouse-report.json
          destination: performance/lighthouse-report.json
  gradual-rollout:
    executor: default
    parameters:
      environment:
        type: enum
        enum: [stg, prd]
      percentage:
        type: integer
      duration_minutes:
        type: integer
    steps:
      - attach_workspace:
          at: .
      - install-bun
      - run:
          name: Gradual rollout << parameters.percentage >>% for << parameters.environment >>
          command: |
            sleep $(( << parameters.duration_minutes >> * 60 ))
  infra-plan:
    executor: default
    parameters:
      environment:
        type: enum
        enum: [rc, stg, prd]
    steps:
      - checkout
      - install-bun
      - run:
          name: Install Pulumi CLI
          command: |
            curl -fsSL https://get.pulumi.com | sh
            echo 'export PATH=$HOME/.pulumi/bin:$PATH' >> $BASH_ENV
            source $BASH_ENV
            pulumi version
      - run:
          name: Install dependencies
          working_directory: infra
          command: bun install --frozen-lockfile --ignore-scripts
      - prepare-infra-env:
          environment: << parameters.environment >>
      - login-pulumi-r2
      - run:
          name: Build infrastructure code
          working_directory: infra
          command: bun run build
      - run:
          name: Pulumi preview for << parameters.environment >>
          working_directory: infra
          command: |
            pulumi stack select "<< parameters.environment >>" --create
            pulumi preview --diff --non-interactive
      - store_artifacts:
          path: infra/Pulumi.yaml
          destination: infra/Pulumi.yaml
  infra-deploy:
    executor: default
    parameters:
      environment:
        type: enum
        enum: [rc, stg, prd]
    steps:
      - checkout
      - install-bun
      - run:
          name: Install Pulumi CLI
          command: |
            curl -fsSL https://get.pulumi.com | sh
            echo 'export PATH=$HOME/.pulumi/bin:$PATH' >> $BASH_ENV
            source $BASH_ENV
            pulumi version
      - run:
          name: Install dependencies
          working_directory: infra
          command: bun install --frozen-lockfile --ignore-scripts
      - prepare-infra-env:
          environment: << parameters.environment >>
      - login-pulumi-r2
      - run:
          name: Build infrastructure code
          working_directory: infra
          command: bun run build
      - run:
          name: Deploy infrastructure to << parameters.environment >>
          working_directory: infra
          command: |
            pulumi stack select "<< parameters.environment >>" --create
            pulumi up --yes --skip-preview --non-interactive
            pulumi stack output --json > stack-outputs.json
            cat stack-outputs.json | jq '.'
      - store_artifacts:
          path: infra/stack-outputs.json
          destination: infra/stack-outputs.json
      - run:
          name: Create GitHub Issue on failure
          when: on_fail
          command: |
            if [ -n "${GITHUB_TOKEN}" ]; then
              curl -X POST \
                -H "Authorization: token ${GITHUB_TOKEN}" \
                -H "Accept: application/vnd.github.v3+json" \
                https://api.github.com/repos/${CIRCLE_PROJECT_USERNAME}/${CIRCLE_PROJECT_REPONAME}/issues \
                -d "{
                  \"title\": \"[Infra Failure] Infrastructure deployment failed on << parameters.environment >>\",
                  \"body\": \"## Infrastructure Deployment Failure\n\n**Environment:** << parameters.environment >>\n**Branch:** ${CIRCLE_BRANCH}\n**Commit:** ${CIRCLE_SHA1}\n**Build:** [#${CIRCLE_BUILD_NUM}](${CIRCLE_BUILD_URL})\n\n### Actions Required\n\n- [ ] Review the Pulumi logs\n- [ ] Check infra/src code changes\n- [ ] Verify Pulumi R2 backend state\n- [ ] Manual rollback if necessary\n\n### Manual Commands\n\n\`\`\`bash\ncd infra\npulumi login \\\"\\\$PULUMI_BACKEND_URL\\\"\npulumi stack select << parameters.environment >>\npulumi preview --diff\npulumi up\n\`\`\`\",
                  \"labels\": [\"infra-failure\", \"<< parameters.environment >>\", \"automated\"]
                }"
            fi
  deploy:
    executor: default
    parameters:
      environment:
        type: enum
        enum: [rc, stg, prd]
    steps:
      - attach_workspace:
          at: .
      - install-bun
      - load-env-config:
          environment: << parameters.environment >>
      - run:
          name: Install Wrangler CLI
          command: |
            bun add -g wrangler@4.54.0
            wrangler --version
      - run:
          name: Setup Cloudflare credentials
          command: |
            export CLOUDFLARE_API_TOKEN="${CLOUDFLARE_API_TOKEN}"
      - run:
          name: Deploy to << parameters.environment >>
          command: |
            MAX_RETRIES=3
            RETRY_COUNT=0

            while [ $RETRY_COUNT -lt $MAX_RETRIES ]; do
              if bun run deploy; then
                break
              else
                RETRY_COUNT=$((RETRY_COUNT + 1))
                if [ $RETRY_COUNT -lt $MAX_RETRIES ]; then
                  sleep 10
                else
                  exit 1
                fi
              fi
            done
          no_output_timeout: 15m
      - run:
          name: Post-deployment verification with auto-rollback
          command: |
            sleep 30

            case "<< parameters.environment >>" in
              rc)
                WEB_URL="https://rc.www.${DOMAIN:-ageha734.jp}"
                API_URL="https://rc.api.${DOMAIN:-ageha734.jp}"
                ;;
              stg)
                WEB_URL="https://stg.www.${DOMAIN:-ageha734.jp}"
                API_URL="https://stg.api.${DOMAIN:-ageha734.jp}"
                ;;
              prd)
                WEB_URL="https://www.${DOMAIN:-ageha734.jp}"
                API_URL="https://api.${DOMAIN:-ageha734.jp}"
                ;;
            esac

            DEPLOYMENT_HEALTHY=true

            if ! curl -sSf "${WEB_URL}/" > /dev/null 2>&1; then
              DEPLOYMENT_HEALTHY=false
            fi

            if ! curl -sSf "${API_URL}/health" | jq -e '.status == "healthy"' > /dev/null 2>&1; then
              DEPLOYMENT_HEALTHY=false
            fi

            if ! bunx playwright test --grep "@smoke" --reporter=line; then
              DEPLOYMENT_HEALTHY=false
            fi

            if [ "${DEPLOYMENT_HEALTHY}" = "false" ]; then
              if ! wrangler pages deployment list --project-name portfolio-web | head -2 | tail -1 | awk '{print $1}' | xargs -I {} wrangler pages deployment rollback --deployment-id {}; then
                exit 1
              fi

              if ! wrangler rollback --name api; then
                exit 1
              fi
              exit 1
            fi
      - run:
          name: Create GitHub Issue on failure
          when: on_fail
          command: |
            if [ -n "${GITHUB_TOKEN}" ]; then
              curl -X POST \
                -H "Authorization: token ${GITHUB_TOKEN}" \
                -H "Accept: application/vnd.github.v3+json" \
                https://api.github.com/repos/${CIRCLE_PROJECT_USERNAME}/${CIRCLE_PROJECT_REPONAME}/issues \
                -d "{
                  \"title\": \"[Deploy Failure] << parameters.environment >> deployment failed on ${CIRCLE_BRANCH}\",
                  \"body\": \"## Deployment Failure Report\n\n**Environment:** << parameters.environment >>\n**Branch:** ${CIRCLE_BRANCH}\n**Commit:** ${CIRCLE_SHA1}\n**Build:** [#${CIRCLE_BUILD_NUM}](${CIRCLE_BUILD_URL})\n\n### Actions Required\n\n- [ ] Review the deployment logs\n- [ ] Check Cloudflare dashboard\n- [ ] Verify Doppler configuration\n- [ ] Consider rollback if necessary\n\n### Rollback Command\n\n\`\`\`bash\nwrangler pages deployment rollback --project-name portfolio-web\n\`\`\`\",
                  \"labels\": [\"deployment-failure\", \"<< parameters.environment >>\", \"automated\"]
                }"
            fi
  backup-database:
    executor: default
    steps:
      - run:
          name: Skip legacy database object-store backup
          command: |
            mkdir -p backup-notes
      - store_artifacts:
          path: backup-notes
          destination: backup-notes
  performance-test:
    executor: large
    steps:
      - checkout
      - install-bun
      - run:
          name: Install Lighthouse CLI
          command: |
            bun add -g lighthouse@12.8.2
            lighthouse --version
      - run:
          name: Run Lighthouse performance tests
          command: |
            URLS=(
              "https://www.ageha734.jp"
              "https://www.ageha734.jp/blog"
              "https://www.ageha734.jp/portfolio"
            )

            mkdir -p performance-reports

            for url in "${URLS[@]}"; do
              SANITIZED_URL=$(echo "${url}" | sed 's/[^a-zA-Z0-9]/-/g')

              lighthouse "${url}" \
                --only-categories=performance,accessibility,best-practices,seo \
                --preset=desktop \
                --chrome-flags="--headless" \
                --output=json \
                --output=html \
                --output-path="./performance-reports/${SANITIZED_URL}" \
                --quiet

              PERFORMANCE_SCORE=$(jq '.categories.performance.score * 100' "./performance-reports/${SANITIZED_URL}.report.json")

              if (( $(echo "${PERFORMANCE_SCORE} < 80" | bc -l) )); then
                exit 1
              fi
            done
      - store_artifacts:
          path: performance-reports
          destination: performance-reports
  cost-report:
    executor: default
    steps:
      - checkout
      - install-bun
      - run:
          name: Generate monthly cost report
          command: |
            MONTH=$(date +%Y-%m)
            REPORT_FILE="cost-report-${MONTH}.md"

            cat > "${REPORT_FILE}" \<<EOF
            # Cost Report - ${MONTH}

            ## Cloudflare Workers

            - Workers Requests: $(curl -s "https://api.cloudflare.com/client/v4/accounts/${CLOUDFLARE_ACCOUNT_ID}/analytics/dashboard" \
              -H "Authorization: Bearer ${CLOUDFLARE_API_TOKEN}" | jq '.result.totals.requests.all')

            ## Cloudflare D1 / KV / R2

            - D1 / KV / R2 usage: Check Cloudflare Dashboard Storage

            ## CircleCI Artifacts

            - Build artifacts via CircleCI store_artifacts

            ## Summary

            - Total Estimated Cost: \$XX.XX USD
            - Trend: [Up/Down/Stable]

            EOF
      - store_artifacts:
          path: cost-report-*.md
          destination: cost-reports
  visual-regression-test:
    executor: large
    steps:
      - checkout
      - install-bun
      - browser-tools/install-chrome
      - run:
          name: Install Percy CLI
          command: |
            bun add -g @percy/cli@1.32.4
            percy --version
      - run:
          name: Run visual regression tests
          command: |
            if [ -n "${PERCY_TOKEN}" ]; then
              export PERCY_TOKEN="${PERCY_TOKEN}"

              bun add -g @percy/playwright@1.1.1
              bunx playwright test --grep "@visual" --reporter=line
            else
              exit 1
            fi
  load-test:
    executor: machine-large
    steps:
      - checkout
      - run:
          name: Run load tests
          command: docker compose -f compose.ci.yaml run --rm k6
          no_output_timeout: 40m
      - store_artifacts:
          path: load-test-results.json
          destination: load-test-results.json

  dast:
    executor: machine-large
    steps:
      - checkout
      - run:
          name: OWASP ZAP baseline
          command: docker compose -f compose.ci.yaml run --rm zap
          no_output_timeout: 30m
      - run:
          name: Nuclei scan
          command: docker compose -f compose.ci.yaml run --rm nuclei
          no_output_timeout: 30m
      - store_artifacts:
          path: zap-report.json
          destination: dast/zap-report.json
      - store_artifacts:
          path: zap-report.html
          destination: dast/zap-report.html
      - store_artifacts:
          path: nuclei-report.jsonl
          destination: dast/nuclei-report.jsonl
workflows:
  ci-cd:
    jobs:
      - setup:
          filters:
            branches:
              ignore:
                - /^copilot\/.*/
      - format:
          requires:
            - setup
      - lint:
          requires:
            - setup
      - typecheck:
          requires:
            - setup
      - test:
          requires:
            - format
            - lint
            - typecheck
      - security-scan:
          requires:
            - setup
          context: security
      - e2e:
          requires:
            - test
            - security-scan
      - build:
          requires:
            - e2e
      - upload-artifacts:
          requires:
            - build
          filters:
            branches:
              only:
                - master
                - main
      - download-artifacts:
          name: download-artifacts-rc
          environment: rc
          requires:
            - upload-artifacts
          filters:
            branches:
              only:
                - master
                - main
      - pre-deploy-check:
          name: pre-deploy-check-rc
          environment: rc
          requires:
            - download-artifacts-rc
          context: env-config
          filters:
            branches:
              only:
                - master
                - main
      - db-migrate:
          name: db-migrate-rc
          environment: rc
          requires:
            - pre-deploy-check-rc
          context: env-config
          filters:
            branches:
              only:
                - master
                - main
      - preview-deploy:
          name: preview-deploy-rc
          environment: rc
          requires:
            - db-migrate-rc
          context:
            - cloudflare
            - env-config
          filters:
            branches:
              only:
                - master
                - main
      - smoke-test:
          name: smoke-test-rc
          environment: rc
          requires:
            - preview-deploy-rc
          context: env-config
          filters:
            branches:
              only:
                - master
                - main
      - validate-deployment:
          name: validate-deployment-rc
          environment: rc
          monitoring_duration: "15m"
          requires:
            - smoke-test-rc
          context:
            - env-config
            - sentry
          filters:
            branches:
              only:
                - master
                - main
      - hold-approval:
          name: hold-approval-rc
          type: approval
          requires:
            - validate-deployment-rc
          filters:
            branches:
              only:
                - master
                - main
      - deploy:
          name: deploy-rc
          environment: rc
          requires:
            - hold-approval-rc
          context:
            - cloudflare
            - env-config
          filters:
            branches:
              only:
                - master
                - main
      - infra-plan:
          name: infra-plan-rc
          environment: rc
          requires:
            - upload-artifacts
          context:
            - pulumi
            - env-config
          filters:
            branches:
              only:
                - master
                - main
      - hold-infra-approval:
          name: hold-infra-approval-rc
          type: approval
          requires:
            - infra-plan-rc
          filters:
            branches:
              only:
                - master
                - main
      - infra-deploy:
          name: infra-deploy-rc
          environment: rc
          requires:
            - hold-infra-approval-rc
          context:
            - pulumi
            - env-config
          filters:
            branches:
              only:
                - master
                - main
      - hold-approval:
          name: hold-approval-stg
          type: approval
          requires:
            - deploy-rc
          filters:
            branches:
              only:
                - master
                - main
      - artifact-promotion:
          name: artifact-promotion-stg
          from_environment: rc
          to_environment: stg
          requires:
            - hold-approval-stg
          filters:
            branches:
              only:
                - master
                - main
      - download-artifacts:
          name: download-artifacts-stg
          environment: stg
          requires:
            - artifact-promotion-stg
          filters:
            branches:
              only:
                - master
                - main
      - pre-deploy-check:
          name: pre-deploy-check-stg
          environment: stg
          requires:
            - download-artifacts-stg
          context: env-config
          filters:
            branches:
              only:
                - master
                - main
      - db-migrate:
          name: db-migrate-stg
          environment: stg
          requires:
            - pre-deploy-check-stg
          context: env-config
          filters:
            branches:
              only:
                - master
                - main
      - preview-deploy:
          name: preview-deploy-stg
          environment: stg
          requires:
            - db-migrate-stg
          context:
            - cloudflare
            - env-config
          filters:
            branches:
              only:
                - master
                - main
      - smoke-test:
          name: smoke-test-stg
          environment: stg
          requires:
            - preview-deploy-stg
          context: env-config
          filters:
            branches:
              only:
                - master
                - main
      - validate-deployment:
          name: validate-deployment-stg
          environment: stg
          monitoring_duration: "30m"
          requires:
            - smoke-test-stg
          context:
            - env-config
            - sentry
          filters:
            branches:
              only:
                - master
                - main
      - deploy:
          name: deploy-stg
          environment: stg
          requires:
            - validate-deployment-stg
          context:
            - cloudflare
            - env-config
          filters:
            branches:
              only:
                - master
                - main
      - hold-approval:
          name: hold-approval-prd
          type: approval
          requires:
            - deploy-stg
          filters:
            branches:
              only:
                - master
                - main
      - artifact-promotion:
          name: artifact-promotion-prd
          from_environment: stg
          to_environment: prd
          requires:
            - hold-approval-prd
          filters:
            branches:
              only:
                - master
                - main
      - download-artifacts:
          name: download-artifacts-prd
          environment: prd
          requires:
            - artifact-promotion-prd
          filters:
            branches:
              only:
                - master
                - main
      - pre-deploy-check:
          name: pre-deploy-check-prd
          environment: prd
          requires:
            - download-artifacts-prd
          context: env-config
          filters:
            branches:
              only:
                - master
                - main
      - db-migrate:
          name: db-migrate-prd
          environment: prd
          requires:
            - pre-deploy-check-prd
          context: env-config
          filters:
            branches:
              only:
                - master
                - main
      - preview-deploy:
          name: preview-deploy-prd
          environment: prd
          requires:
            - db-migrate-prd
          context:
            - cloudflare
            - env-config
          filters:
            branches:
              only:
                - master
                - main
      - smoke-test:
          name: smoke-test-prd
          environment: prd
          requires:
            - preview-deploy-prd
          context: env-config
          filters:
            branches:
              only:
                - master
                - main
      - validate-deployment:
          name: validate-deployment-prd
          environment: prd
          monitoring_duration: "1h"
          requires:
            - smoke-test-prd
          context:
            - env-config
            - sentry
          filters:
            branches:
              only:
                - master
                - main
      - deploy:
          name: deploy-prd
          environment: prd
          requires:
            - validate-deployment-prd
          context:
            - cloudflare
            - env-config
          filters:
            branches:
              only:
                - master
                - main
      - hold-infra-approval:
          name: hold-infra-approval-stg
          type: approval
          requires:
            - infra-deploy-rc
          filters:
            branches:
              only:
                - master
                - main
      - infra-plan:
          name: infra-plan-stg
          environment: stg
          requires:
            - hold-infra-approval-stg
          context:
            - pulumi
            - env-config
          filters:
            branches:
              only:
                - master
                - main
      - infra-deploy:
          name: infra-deploy-stg
          environment: stg
          requires:
            - infra-plan-stg
          context:
            - pulumi
            - env-config
          filters:
            branches:
              only:
                - master
                - main
      - hold-infra-approval:
          name: hold-infra-approval-prd
          type: approval
          requires:
            - infra-deploy-stg
          filters:
            branches:
              only:
                - master
                - main
      - infra-plan:
          name: infra-plan-prd
          environment: prd
          requires:
            - hold-infra-approval-prd
          context:
            - pulumi
            - env-config
          filters:
            branches:
              only:
                - master
                - main
      - hold-infra-approval:
          name: hold-infra-deploy-prd
          type: approval
          requires:
            - infra-plan-prd
          filters:
            branches:
              only:
                - master
                - main
      - infra-deploy:
          name: infra-deploy-prd
          environment: prd
          requires:
            - hold-infra-deploy-prd
          context:
            - pulumi
            - env-config
          filters:
            branches:
              only:
                - master
                - main
  backup:
    triggers:
      - schedule:
          cron: "0 2 * * *"
          filters:
            branches:
              only:
                - master
    jobs:
      - backup-database
  performance:
    triggers:
      - schedule:
          cron: "0 3 * * 1"
          filters:
            branches:
              only:
                - master
    jobs:
      - performance-test
  monthly-reports:
    triggers:
      - schedule:
          cron: "0 9 1 * *"
          filters:
            branches:
              only:
                - master
    jobs:
      - cost-report:
          context:
            - cloudflare
  visual-regression:
    triggers:
      - schedule:
          cron: "0 4 * * 0"
          filters:
            branches:
              only:
                - master
    jobs:
      - visual-regression-test:
          context: percy
  load-testing:
    triggers:
      - schedule:
          cron: "0 5 * * 0"
          filters:
            branches:
              only:
                - master
    jobs:
      - load-test
  dast-stg:
    triggers:
      - schedule:
          cron: "0 6 * * 0"
          filters:
            branches:
              only:
                - master
    jobs:
      - dast
